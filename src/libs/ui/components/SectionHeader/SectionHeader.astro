---
import { LIB_PREFIX } from '../../config';

interface Props {
  class?: string;
  alignment?: 'left' | 'center' | 'right';
}

const { class: className = '', alignment = 'left' } = Astro.props;
---

<style lang="scss">
  @use './SectionHeader.scss';
</style>

<div class={`${LIB_PREFIX}section-header align-${alignment} ${className}`}>
  <slot name="pretitle" />
  <slot name="title" />
  <slot name="description" />
</div>
