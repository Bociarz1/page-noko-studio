---
import { Image } from 'astro:assets';
import Button from '@components/shared/Button/Button.astro';

interface Props {
  preTitle: string;
  title: string;
  description: string;
  image: {
    src: ImageMetadata;
    alt: string;
    sizes: string;
  };
  height?: string;
}

const { preTitle,title, description, image, height } = Astro.props;
const {src, alt, sizes} = image;
---

<style lang="scss">
  @use './Hero.scss';
</style>

<section class="hero" style={height ? `--hero-height: ${height}` : null}>
  <Image
    src={src}
    alt={alt}
    class="hero__bg"
    width={1920}
    height={1080}
    format="avif"
    quality="mid"
    loading="eager"
    fetchpriority="high"
    decoding="sync"
    widths={[768, 1280, 1920]}
    sizes={sizes}
  />
  <div class="hero__overlay"></div>
  <div class="hero__content">
    <h1 class="hero__pretitle page-pretitle">
      {preTitle}
    </h1>
    <p class="hero__title section-title">
      {title}
    </p>
    <p class="hero__desc section-description">
      {description}
    </p>
    <div class="hero__actions">
      <slot/>
    </div>
  </div>
</section>
