---
import { NavLink } from 'src/constants/navLinks';

interface Props {
  navElements: NavLink[];
}
const { navElements = [] } = Astro.props;
---

<style lang="scss">
  @use '@components/shared/Header/Header.scss';
  @use './HeaderMenu.scss';
</style>

<ul class="header-menu-desktop">
  {
    navElements.map((element) => (
      <li class={element?.isPathActive ? 'link--active' : ''}>
        <a href={element.href}>{element.label}</a>
      </li>
    ))
  }
</ul>